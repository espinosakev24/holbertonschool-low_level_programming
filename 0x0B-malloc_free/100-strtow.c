#include <stdio.h>
#include <stdlib.h>
/**
 * strtow - prints a grid of integers
 * @str: pointer
 *
 * Return: Nothing.
 */
char **strtow(char *str)
{
	if (*str == NULL || *str == "")
	{
		return (NULL);
	}
}
