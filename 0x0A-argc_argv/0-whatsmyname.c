#include <stdio.h>
/**
 * main - check the code for Holberton School students.
 * @argc: counter of number of parameters of a command
 * @argv: array of pointers
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{

	if (argc != 0)
	{
		printf("%s\n", argv[0]);
	}
return (0);
}
