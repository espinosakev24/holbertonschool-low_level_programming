#include "holberton.h"
#include <stdio.h>
/**
 * _abs - check the code for Holberton School students.
 * @n: abs value
 * Return: Always abs value
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
