#ifndef HOLBERTONSCHOOL
#define HOLBERTONSCHOOL
#include <stdio.h>
int linear_search(int *array, size_t size, int value);
int binary_search(int *array, size_t size, int value);
void PrintArray(int *array, int index, int size);
#endif
