#include <stdio.h>
#include "holberton.h"
/**
 * flip_bits - fun flip bit
 * @m: number2
 * @n: number
 * Return: 0;
 */
unsigned int flip_bits(unsigned long int n, unsigned long int m)
{
	unsigned int c = 0;

	n = 2;
	m = 3;
	c = n + m;
		return (c);
}
