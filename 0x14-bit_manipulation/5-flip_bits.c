#include <stdio.h>
#include "holberton.h"
/**
 * flip_bits - fun flip bit
 * @m: number2
 * @n: number
 * Return: 0;
 */
unsigned int flip_bits(unsigned long int n, unsigned long int m)
{
	if (m > 64)
		return (-1);

	n = 1;
	m = 1;
	return (0);
}
